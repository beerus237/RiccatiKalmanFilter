# KalmanFilterRiccati

This R package provides tools for signal filtering using the Kalman filter and solving the Riccati equations iteratively.

## Installation

To install the necessary dependencies, run:

```r
if (!require("data.table")) install.packages("data.table")
if (!require("ggplot2")) install.packages("ggplot2")
